# @package _global_

# specify here default configuration
# order of defaults determines the order in which configs override each other
defaults:
  - launcher: null
  - platform: gpu
  - paths: default
  - hydra: default
  - _self_
task_name: md
pdb_dir: ${paths.data_dir}/pdbs
output_dir: ${paths.data_dir}/md/${pdb_filename}_${temperature}_${frame_interval}
pdb_filename: null
temperature: 310
warmup_steps: 50_000 # How many integration steps to take before recording frames
frame_interval: 10_000 # How many integration steps between recorded frames
num_frames: 10_000 # How many frames to record
log_freq: 100 # How often to log simulation data (in integration steps)
frames_per_save: 1000 # How many frames to save at once, shape (frames_per_save, num_atoms, 3)
